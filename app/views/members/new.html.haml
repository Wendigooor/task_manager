%h1= t('.title')

= simple_form_for @user, url: family_members_path do |f|
  = f.input :id, collection: User.all - @family.users, label_method: :email, label: false, value_method: :id, prompt: :translate, required: false
  = f.input :email, required: true, label: false
  = f.input :role, as: :radio_buttons, collection: User::ROLES, required: true, checked: User::ROLES.first
  = f.button :submit
