- if controller_name != 'sessions'
  - if user_signed_in?
    = link_to "Edit profile", edit_user_registration_path |
    = link_to('Not you? Logout', destroy_user_session_path)
  - else
    = link_to('Login', new_user_session_path)
